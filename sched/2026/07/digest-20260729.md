# Linux Kernel sched 子系统日报 · 2026-07-29

## 一、概览

当天捕获 sched 相关邮件 **40 封**，聚合为 17 个线程，其中 **8 个核心系列**生成分析文章：

- **fix（修复）**：3 篇 — dl_server wakeup rule、idle tick 回退修复、avg_idle idle_stamp 修复
- **feature（特性/优化）**：5 篇 — NOHZ 均衡偏好全空闲 core、sched/cache v8、per-CPU sched debugfs、perf sched latency v4、cpuidle QoS 缓存

另有 octeontx2 网卡 TX 调度、drm/imagination、intel_pstate、CPPC、amd-pstate EPP boost RFC 等边缘线程未成文（非 kernel/sched 核心）。

## 二、参与机会

1. **[001] sched/fair: NOHZ 均衡偏好全空闲 core**（Andrea Righi，v1）— 可在 SMT 平台复测 GEMM/schbench 数据回帖；Prateek 提的遍历剪枝优化也可帮忙实现验证。
2. **[003] sched/idle: 无 cpuidle driver 时停 tick**（Christian Loehle）— 可在虚机/云环境复测 sysbench 回退恢复情况，补 Tested-by。
3. **[005] sched/cache: task_cache_work 降开销 v8**（Luo Gengkun）— Chen Yu 判定并发窗口可容忍但要求补注释，可 review 注释措辞或在 NUMA 机器压测。
4. **[006] sched/debug: per-CPU debugfs**（Aaron Tomlin，v2）— offline CPU 返回 -ENODEV 的语义、possible vs online 建目录问题都值得回帖；作者关于 pre-existing issues 的提问目前无人回应。
5. **[007] perf sched latency v4**（Aaron Tomlin）— pipe mode（-i -）实测反馈对合入很有帮助，门槛低。
6. **[008] cpuidle: QoS 约束缓存 v2**（Yaxiong Tian）— 系列零回复，第一个独立 ftrace 复测数据价值很高；也可 review QoS notifier 失效路径完整性。

## 三、合入进展（likelihood 为 high 的系列）

- **[002] sched/deadline: revised wakeup rule 仅用于 running dl_server** — Peter Zijlstra 直接问 "sched/urgent this?"，大概率近期走 tip/sched/urgent。
- **[003] sched/idle 单行回退修复** — 修复明确回退（OCI sysbench -29%），有机理分析支撑，合入概率高。
- **[004] sched/core: avg_idle 无效 idle_stamp 跳过更新** — 带 Fixes 标签 + Prateek Reviewed-by，走 urgent 通道概率高。
- **[007] perf sched latency v4** — 工具类改动、逐条回应意见、无争议，等 perf 维护者确认。

## 四、归档情况

- 本地归档：`sched/2026/07/` 8 篇 + `index-20260729.md`，by-tag 索引已重建（总计 15 篇、15 个标签、无非法标签）
- IMA 知识库：8/8 上传成功
- GitHub：已推送（commit "sched digest 20260729"）

## 五、遇到的问题

- 邮件正文抓取 0 失败（40/40 成功）。
- 首次拉取清单时 receive.js 超时（全量清单约 6000 封需 ~3 分钟），已修复 skill 脚本：该步骤超时提高到 900s 且空输出不再静默当作零封。
- [008] cpuidle 系列的 patch 1/6 未出现在当天邮件缓存中，文章已如实标注。

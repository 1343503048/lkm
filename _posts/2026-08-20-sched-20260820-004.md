---
id: sched-20260820-004
date: 2026-08-20
subsystem: sched
type: bug
status: under_review
severity: low
thread_root_msgid: <unknown>
lore_url: 未获取到
authors:
- kernel test robot
maintainers_involved:
- Peter Zijlstra
- Vincent Guittot
current_version: report
patch_series:
- version: report
  msgid: <unknown>
  date: 2026-08-20
  summary: LKP sparse 警告：kernel/sched/fair.c 第 2004 行（enqueue 路径判断 cfs_rq->nr_running）疑似用错了变量/类型，疑似
    commit 85570f10a4c6（把 EEVDF 的 runqueue 从 multi 改为 single）引入。无修复补丁，仅静态检查告警。
  review_outcome: LKP 自动报告，暂无维护者回复或 fix。
upstream_commit: null
fixes_commit: '85570f10a4c6 ("sched/fair: Consolidate single-entity load tracking
  to a single runqueue")'
merged_branch: null
merge_assessment:
  likelihood: pending
  blocking_issues:
  - 需确认是真实 bug 还是 sparse 误报；85570f10a4c6 是近期热点 commit（同日 flat-hierarchy bug 也指向它）
  next_action: 等待维护者确认变量语义；可能与 08-20 001/flat-hierarchy 线的 85570f10a4c6 关注点一并排查。
contribution_opportunities:
- kind: discussion
  description: 可帮忙复现 sparse 警告、确认 nr_running 在 single-runqueue 改写下的正确性，或给出最小 fix。
generated_at: '2026-08-21T00:30:00'
source_email_count: 1
related_articles:
- sched-20260819-001
tags:
- sched/fair
- regression
title: LKP sparse 在 `kernel/sched/fair.c:2004`（enqueue 路径判断 `cfs_rq->nr_running`）发出静...
layout: article
---

## TL;DR
LKP sparse 在 `kernel/sched/fair.c:2004`（enqueue 路径判断 `cfs_rq->nr_running`）发出静态检查告警，疑似近期 commit `85570f10a4c6`（EEVDF single runqueue 合并）引入。无修复补丁，仅自动报告。

## 背景与问题
LKP 自动 sparse 扫描报告 `fair.c:2004` 处类型/变量 mismatch 警告。commit `85570f10a4c6`（把 EEVDF 的多 runqueue 负载追踪 Consolidate 到 single runqueue）是近期改动热点——同日 flat-hierarchy 除零 bug（article 001）也指向该 commit 引入的 `enqueue_hierarchy()` 缺少 `se->on_rq` 守卫。本 sparse 告警疑似同一重构引入的遗留不一致。

## 技术方案
暂无补丁。需先判定是真实语义错误还是 sparse 误报：`enqueue` 路径上 `nr_running` 的读取在 single-runqueue 模型下是否仍正确。

## 版本演进与当前进展
- 2026-08-20 LKP 自动报告，无回复、无 fix。

## Maintainer 意见与讨论焦点
暂无。建议与同日 flat-hierarchy 线（均指向 85570f10a4c6）一并排查，避免遗漏同一重构的其它退化。

## 合入评估
合入可能性 pending：取决于是否确认为真实 bug。若为真实问题，预期会有最小 fix；若误报则关闭。

## 效果评估
无性能数据（静态检查告警）。

## 我可以参与的点
- 复现 sparse 警告（需开启相应 sparse 检查），确认是否真实语义错误并草拟最小 fix，回帖到 LKP 线程。

## 参考链接
- lore thread: 未获取到
- 关联 commit: 85570f10a4c6（同日 flat-hierarchy 除零 bug 也指向此 commit）

---
id: sched-20260820-008
date: 2026-08-20
subsystem: sched
type: fix
status: merged_tip
severity: low
thread_root_msgid: <unknown>
lore_url: 未获取到
authors:
- Liang Luo
- Tejun Heo
maintainers_involved:
- Tejun Heo
- Changwoo Lee
current_version: applied
patch_series:
- version: applied
  msgid: <unknown>
  date: 2026-08-20
  summary: sched-ext.rst 示例 ei->type → ei->kind 修复已 Applied 到 sched_ext/for-7.3-fixes（对应
    08-19 008 之二）。同日 Liang Luo『document that cgroup CPU knobs are scheduler-dependent』发
    v2（对应 08-19 008 之一 + 08-20 003 的文档线）：sched-ext.rst 明确 CPU knob 仅当调度器实现对应回调才生效。
  review_outcome: Fixes fa48e8d2c7b5 的 ei->kind 修复已 Applied；v2 文档补丁在 review 中。
upstream_commit: 已 Applied 到 sched_ext/for-7.3-fixes
fixes_commit: 'fa48e8d2c7b5 ("sched_ext: Documentation: scheduler: Document extensible
  scheduler class")'
merged_branch: sched_ext/for-7.3-fixes
merge_assessment:
  likelihood: merged
  blocking_issues:
  - ei->kind 修复已 Applied；v2 doc 补丁待最终 ack
  next_action: v2 doc 补丁随 review 收下；ei->kind 已进 for-7.3-fixes。
contribution_opportunities:
- kind: review
  description: 可帮忙 ack v2『cgroup CPU knobs scheduler-dependent』文档表述。
generated_at: '2026-08-21T00:30:00'
source_email_count: 2
related_articles:
- sched-20260819-008
- sched-20260820-003
tags:
- sched_ext
- documentation
- cgroup
title: 08-19 的 sched_ext 文档两连修在 08-20 推进：① `ei->type → ei->kind` 示例修复已 Applied 到 `sc...
layout: article
---

## TL;DR
08-19 的 sched_ext 文档两连修在 08-20 推进：① `ei->type → ei->kind` 示例修复已 Applied 到 `sched_ext/for-7.3-fixes`（Fixes `fa48e8d2c7b5`）；② `cgroup CPU knobs are scheduler-dependent` 文档补丁发 v2。

## 背景与问题
- `Documentation/scheduler/sched-ext.rst` 的 `ops.exit()` 示例读 `ei->type`，但 `struct scx_exit_info` 从引入起就是 `ei->kind`（无 `type` 字段）。照抄示例的调度器编译失败。Fixes `fa48e8d2c7b5`。
- `cgroup-v2.rst` 的 `cpu.max`/`cpu.idle` 仅写 "affects only fair-class"，但 sched_ext 通过 `cgroup_set_bandwidth`/`cgroup_set_idle` 透传，BPF 用户易错过这些通知点。

## 技术方案
- sched-ext.rst：`exit_type = ei->type;` → `exit_type = ei->kind;` —— 已 Applied。
- v2 文档补丁：sched-ext.rst 明确 "CPU knob 仅当加载的调度器实现了对应回调才生效"，与 08-20 003（Michal 的 cgroup-v2.rst 补丁）互为佐证。

## 版本演进与当前进展
- 08-19：Liang Luo 两封文档补丁发出。
- 08-20：ei->kind 修复 Applied 到 for-7.3-fixes；cgroup knobs doc 发 v2。

## Maintainer 意见与讨论焦点
无争议（文档方向已由 Tejun 在 08-19 裁定为「文档而非运行时警告」）。焦点在措辞一致性（cgroup-v2.rst 与 sched-ext.rst 两侧）。

## 合入评估
ei->kind 修复已 merged（Applied 到 for-7.3-fixes）。v2 doc 补丁合入可能性 high（纯文档，方向与 08-20 003 一致）。

##  效果评估
无性能数据（文档）。

## 我可以参与的点
- 可作为 reviewer 确认两处文档措辞一致；或检查 sched-ext.rst 是否还有其它过时字段引用。

## 参考链接
- lore thread: 未获取到
- Fixes: fa48e8d2c7b5
- 关联：sched-20260819-008、sched-20260820-003
